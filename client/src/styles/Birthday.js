import styled from "styled-components";

const Birthday = styled.section `
    width: 300px;
    height: 360px;
    margin-top: 50px;
    background: red;
    text-align: center;
    outline: 1px dashed #fff;
    outline-offset: -10px;
`;

export default Birthday;